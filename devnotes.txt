Store:

boards: {
  state: {
    boards: {},
  },
  actions: {
    /* No such thing like "currentBoard" */
    loadAllBoards(),  // Thunk (calls boardsAdded)
    createBoard(),  // Thunk (calls boardsAdded)
    boardsAdded(),
    updateBoard(),  // Thunk (calls boardUpdated)
    boardUpdated(),
    loadSomeBoardColumnCards()  // Thunk (calls boardUpdated)
    loadCardData(),   // Thunk (calls loadSomeCardComments)
    loadSomeCardComments()  // Thunk (calls boardUpdated)
  }
}
users: {
  state: {
    users: {}
  },
  actions: {
    addUser(),  // Thunk (calls usersAdded)
    usersAdded(),
    loadUsers(),  // Thunk (calls usersAdded)
    updateUser(),
    clearUsers(),
    deactivateUser()  // Thunk (calls clearUsers)
  }
}

// todo: state selectors

possible fetching statuses:
- not started at all
- in progress
- success
- failed
- success, but in a situation we might want to refetch ("needs refetch")